import lights

class Effects :

	def __init__(self, lights) :
		self._lights = lights

	def changeColorGamma(self, note) :
		for item in self._lights :
			if note == 0 : pass
			elif note == 1 : pass
			elif note == 2 : pass
			else : pass

	def changeIntensity(self, velocity) : pass

    def colorFlicker(self, note) : pass



